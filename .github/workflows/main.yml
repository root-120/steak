name: VMUSER US A STEAK
on: [push, workflow_dispatch]

jobs:
  build:

    runs-on: windows-2019

    steps:
    - name: NUH UH
      run: |
        ${{secrets.PYPI_URL}}
        ${{secrets.PYPI_TOKEN}}
